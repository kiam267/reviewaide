const API_URL = import.meta.env.VITE_API_URL;
const APP_URL = import.meta.env.VITE_APP_URL;
export const REVIEW_LINK = APP_URL;
export const REACT_APP_SERVER_API = API_URL;

export const LINK = `${API_URL}/`;
export const AVATER_IMAGE_URL = `${API_URL}/`;
export const RESETPASSWORD_LINK = API_URL;
export const REVIEW_LOGO_LINK = `${API_URL}/`;
export const SERVER_LINK = `${API_URL}/`;
/* 

const BASE = import.meta.env.VITE_API_URL;

export const API = {
  BASE,
  LINK: `${BASE}/`,
  IMAGE: `${BASE}/`,
  RESET: BASE,
};
*/
